# TARKAN — CPU-first, T4-compatible. Install torch from the CPU index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1,<2.4
transformers>=4.40,<4.46
tokenizers>=0.19
huggingface_hub>=0.23
accelerate>=0.30
# bitsandbytes>=0.43   # GPU-only (4-bit teacher). Installed on the T4 server, not on CPU.
sentencepiece
emoji<2.0               # BERTweet TweetNormalizer dependency
spacy>=3.7
# en_core_web_sm via: python -m spacy download en_core_web_sm
pandas>=2.0
pyarrow>=15
numpy>=1.24,<2.0
scikit-learn>=1.3
scipy>=1.10
seqeval>=1.2
Pillow>=10
matplotlib>=3.7
seaborn>=0.13
pyyaml>=6
python-dotenv>=1.0
tqdm>=4.66
# --- KAN backend ---
# Default (B-spline, faithful to Eq. 19), install separately (not on PyPI):
#   pip install git+https://github.com/Blealtan/efficient-kan.git
# If efficient_kan is unavailable, kan_fusion.py falls back to a vendored RBF-KAN
# (genuine KAN, zero extra deps). Optional alternatives:
#   pip install rkan        # rational-KAN ablation (paper ref [41])
#   pip install fastkan     # RBF KAN (NOTE: not reliably on PyPI; install from source if needed)
pytest>=8
