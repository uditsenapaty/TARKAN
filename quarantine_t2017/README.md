# QUARANTINED — t2017 scoring artifacts, DO NOT USE

These are t2015-trained members scored read-only on t2017 (§D.18), used once to test
whether the CER effect replicated. Nothing here was trained on t2017 and no reported
t2015 number depends on it: the CER configuration was already rejected by t2015 dev alone
(§D.17 — dev falls 70.43 -> 70.34 -> 69.74 as CER members are added).

Kept only so the t2017 exposure is documented rather than hidden. t2015 and t2017 are to
be run as SEPARATE experiments; do not use these to select anything.
